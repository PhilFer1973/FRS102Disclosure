{
  "name": "frs102-reviewer-mcp-app",
  "version": "0.1.0",
  "type": "module",
  "description": "FRS 102 Disclosure Reviewer — MCP app for Claude Desktop",
  "private": true,
  "scripts": {
    "build:ui": "cross-env INPUT=mcp-app.html vite build",
    "build:server": "esbuild main.ts --bundle --platform=node --format=esm --packages=external --outfile=dist/server.mjs",
    "build": "npm run build:ui && npm run build:server",
    "stdio": "tsx main.ts",
    "dev": "cross-env NODE_ENV=development INPUT=mcp-app.html vite build --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/ext-apps": "^1.7.0",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^4.1.13"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "cross-env": "^10.1.0",
    "esbuild": "^0.28.1",
    "tsx": "^4.19.0",
    "typescript": "^5.9.3",
    "vite": "^6.0.0",
    "vite-plugin-singlefile": "^2.3.0"
  }
}
